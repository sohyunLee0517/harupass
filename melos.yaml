name: harupass

packages:
  - packages/*

sdkPath: auto

command:
  bootstrap:
    usePubspecOverrides: true

scripts:
  analyze:
    run: dart analyze .
    exec:
      concurrency: 5
    packageFilters:
      flutter: true

  test:
    run: flutter test
    exec:
      concurrency: 5
    packageFilters:
      dirExists: test

  clean:
    run: flutter clean
    exec:
      concurrency: 5

  build:android:admin:
    run: flutter build apk --release
    packageFilters:
      scope: admin_app

  build:android:subject:
    run: flutter build apk --release
    packageFilters:
      scope: subject_app
